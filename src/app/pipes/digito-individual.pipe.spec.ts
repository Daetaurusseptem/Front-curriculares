import { DigitoIndividualPipe } from './digito-individual.pipe';

describe('DigitoIndividualPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitoIndividualPipe();
    expect(pipe).toBeTruthy();
  });
});
