import { GenerarfechaPipe } from './generarfecha.pipe';

describe('GenerarfechaPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerarfechaPipe();
    expect(pipe).toBeTruthy();
  });
});
